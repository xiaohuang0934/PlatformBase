{
  // ========== 开发环境专属配置 ==========
  // 此文件仅当 ASPNETCORE_ENVIRONMENT=Development 时加载
  // 会覆盖 appsettings.json 中的同名配置

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // 开发环境开启SQL敏感数据日志，便于调试
  "Database": {
    "EnableSensitiveDataLogging": true
  }
}
